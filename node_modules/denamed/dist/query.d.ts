import { DnsAnswer, DnsMessage, DnsResponseMessage } from "./core/protocol";
export declare function createResponse(query: DnsMessage, answers: DnsAnswer[]): DnsResponseMessage;
export interface QuerySource {
    family: "udp6";
    address: string;
    port: number;
}
